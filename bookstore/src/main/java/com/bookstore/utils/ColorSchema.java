package com.bookstore.utils;

import java.awt.Color;

// Define Color for label, panel, warning, header,...
public class ColorSchema {

    // Admin
    public static final Color ADMIN_PRIMARY = Color.decode("#1a237e");      // Indigo đậm
    public static final Color ADMIN_SECONDARY = Color.decode("#283593");    // Indigo
    public static final Color ADMIN_ACCENT = Color.decode("#3949ab");       // Indigo nhạt
    public static final Color ADMIN_BACKGROUND = Color.decode("#e8eaf6");   // Indigo rất nhạt

    // Employee
    public static final Color EMPLOYEE_PRIMARY = Color.decode("#00695c");   // Teal đậm
    public static final Color EMPLOYEE_SECONDARY = Color.decode("#00796b"); // Teal
    public static final Color EMPLOYEE_ACCENT = Color.decode("#00897b");    // Teal nhạt
    public static final Color EMPLOYEE_BACKGROUND = Color.decode("#e0f2f1");// Teal rất nhạt

    // Customer
    public static final Color CUSTOMER_PRIMARY = Color.decode("#ad1457");   // Pink đậm
    public static final Color CUSTOMER_SECONDARY = Color.decode("#c2185b"); // Pink
    public static final Color CUSTOMER_ACCENT = Color.decode("#d81b60");    // Pink nhạt
    public static final Color CUSTOMER_BACKGROUND = Color.decode("#fce4ec");// Pink rất nhạt

    // Text Color
    public static final Color TEXT_DARK = Color.decode("#263238");          // Xanh gần đen
    public static final Color TEXT_LIGHT = Color.decode("#ffffff");         // Trắng
    public static final Color TEXT_MUTED = Color.decode("#607d8b");         // Xanh xám

    // Dialog
    public static final Color SUCCESS = Color.decode("#4caf50");            // Xanh lá (Thành công)
    public static final Color ERROR = Color.decode("#f44336");              // Đỏ (Lỗi)
    public static final Color WARNING = Color.decode("#ff9800");            // Cam (Cảnh báo)
    public static final Color INFO = Color.decode("#2196f3");               // Xanh dương (Thông tin)

    // Border and Header
    public static final Color BORDER_LIGHT = Color.decode("#e0e0e0");       // Xám nhạt
    public static final Color HEADER_GRADIENT_START = Color.decode("#f5f5f5"); // Xám rất nhạt
    public static final Color HEADER_GRADIENT_END = Color.decode("#e0e0e0");   // Xám nhạt

}
